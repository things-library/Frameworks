// ================================================================================
// <copyright file="MemoryMetrics.cs" company="Starlight Software Co">
//    Copyright (c) 2025 Starlight Software Co. All rights reserved.
//    Licensed under the Apache License, Version 2.0. See LICENSE in the project root for license information.
// </copyright>
// ================================================================================

using ThingsLibrary.Metrics;

namespace ThingsLibrary.Tests.Metrics
{
    [TestClass, ExcludeFromCodeCoverage]
    public class MemoryMetricsTests
    {
        //[TestMethod]
        //public void Constructor()
        //{
        //    var snapshot = MemoryMetrics.GetSnapshot();
        //    Assert.IsTrue(snapshot != null);

        //    var total = snapshot.TotalKB;
        //    Assert.IsTrue(total > 0);

        //    var avail = snapshot.AvailableKB;
        //    Assert.IsTrue(avail > 0);

        //    var used = snapshot.UsedKB;
        //    Assert.IsTrue(used > 0);

        //    Assert.AreEqual(avail / (decimal)total, snapshot.AvailablePercent);
        //    Assert.AreEqual(used / (decimal)total, snapshot.UsedPercent);
        //}
    }
}
